% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dt_keeprownames.R
\name{dt_keeprownames}
\alias{dt_keeprownames}
\title{Create data.table from data.frame with row names}
\usage{
dt_keeprownames(df, check.names=FALSE, key = NULL, stringsAsFactors = FALSE)
}
\arguments{
\item{df}{a \code{data.frame}}

\item{check.names}{same as in data.frame}

\item{key}{character vector of one or more column names which is passed to setkey}

\item{stringsAsFactors}{same as in data.table}
}
\value{
returns a \code{data.table}
}
\description{
This a wrapper function around \code{data.table(df, keep.rownames=TRUE)}

For more information about \code{data.table} and \code{data.frame} \href{http://www.google.com}{Google it}.
}
\examples{
dt_keeprownames(mtcars)

\dontrun{
dt_keeprownames(data.table(mtcars)) # this will throw an error because input is not a data.frame
df <- mtcars
rownames(df) <- NULL
dt_keeprownames(df)
# if data.frame has no row names, the returned data.table will create column rn
with row index numbers
}

}
